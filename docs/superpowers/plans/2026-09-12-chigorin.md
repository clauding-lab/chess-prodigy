# Chigorin Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans task-by-task.

**Goal:** Add a distinct, historically grounded and freshly measured Chigorin opponent in the actual app.
**Architecture:** Separate corpus importer and compact book; separate Chigorin plan policy using existing neutral root search. Exact opponent identity flows through current game, storage and accounts with new recovery generations and capability floor.
**Tech Stack:** TypeScript, React, Vitest, Playwright, Express/SQLite; existing dependencies only.
**Spec:** docs/superpowers/specs/2026-09-12-chigorin-design.md

## Global Constraints

- Preserve Classic and every Morphy playing/rating policy and exact legacy wire acknowledgement.
- Chigorin identity: chigorin/version1/chigorin-plans-v1/seeded-per-ply-v1.
- Style guard100cp, bonus cap240cp, shared production deadlines including30ms ranking reserve.
- New authority state-v6/account-v6/history-v5; wire schema2; permanent minimum policy4 after Chigorin.
- Dark default, explicit updates, Home/Resume and no real player data.
- Measure frozen production policy before rated setup, first eligible50/100/200-pair checkpoint; no reused ratings.

### Task 1: Corpus and playing policy
Files: new scripts/chigorin-history/{corpus,import}.ts, src/book/chigorin-book.json,
docs/verification/chigorin/{corpus,behaviour-protocol,behaviour-report}.md and source artifacts,
src/engine/chigorin.ts, tests/engine/chigorin.test.ts; modify src/engine/{opponents,morphy}.ts.
Produces chigorinConfig(seed), chooseChigorinMove(position,level,seed,ply,now?),
explainChigorinMove(...) with result, plan, progress, depth, neutralLoss, reason.
- [ ] Declare off-book fixtures and intended knight/centre/attack/tactical behavior before policy.
- [ ] Write tests first: `expect(isSupportedOpponent(chigorinConfig(7))).toBe(true)`;
  recorded choices must occur in corpus, unique mates must win, style neutralLoss<=100,
  terminal move null, invalid identity rejected. Run focused tests and retain initial missing-feature failures.
- [ ] Import exact historical player games, exclude malformed/nonordinary records, assert every book move legal.
- [ ] Implement deterministic book and bounded position-derived policy; dispatch Chigorin by id before Morphy version.
- [ ] Run focused engine/corpus tests and behavior report; commit; independent review before freezing.

### Task 2: Fresh measurement
Files: scripts/calibration/{protocol,match,run}.ts; tests/calibration/matches.test.ts;
docs/verification/chigorin/calibration/ and measurement-report.md.
Consumes chooseOpponentMove and exact Chigorin identity. Produces accepted fixed ratings only from eligible evidence.
- [ ] Add failing protocol tests: `expect(opponentIdentity(CHIGORIN_PROTOCOL).id).toBe('chigorin')`;
  `expect(openingForProtocol(CHIGORIN_PROTOCOL,1)).toEqual([])`; reject mismatched saved opponent.
- [ ] Add protocol selection and production dispatch preserving old protocols; fingerprint influencing source.
- [ ] Freeze reviewed source with git archive and hash inventory, use locked dependencies.
- [ ] Run50 pairs per level using existing run/worker scripts, verify saved matches; extend only to100/200 if needed.
- [ ] Independently replay games/check hashes/recompute intervals; retain compressed raw games and checkpoint summaries.

### Task 3: Actual app and compatibility
Files: src/rating/opponents.ts, src/ui/{Home,Modals}.tsx, src/App.tsx, src/game/*,
src/storage/{store,history}.ts, src/account/sync.ts, server/records.ts;
tests/{ui,storage,account,server,game,browser} Chigorin/regression checks.
Consumes measured ratings and exact engine identity. Produces safe selectable/resumable Chigorin.
- [ ] Write failing tests for new saved identity, rating receipts, old generations and policy4 rejection/reset persistence.
- [ ] Add exact id-based ratings, measured setup and Home option; maintain old opponent rematches unchanged.
- [ ] Advance local keys and minimum-policy mapping; preserve originals/pending overlays/raw acknowledgements.
- [ ] Test old-client rejection after assisted Chigorin, resets and reloads; game/archive/replay/rematch/rating exclusions.
- [ ] Real enabled browser journeys for both colors, all levels, accounts/guest/offline/Resume and mobile layout.

### Task 4: Whole-app verification and documentation
Files: package.json/package-lock.json, README.md, CHANGELOG.md, AGENTS.md, VISION.md,
docs/verification/chigorin/integration-report.md and release-report.md.
- [ ] Run npm test, typecheck, lint, format:check, enabled build/browser suite; default build/browser coverage.
- [ ] Run accessibility in both themes and inspect actual UI, preserve installed-version/About behavior.
- [ ] Independent TypeScript/security/code review of whole branch; fix findings and targeted recheck.
- [ ] Record exact evidence, unresolved limits and local build status; commit verified2.5.0 candidate.
