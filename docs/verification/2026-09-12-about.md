# About panel — 12 September 2026 BDT

Application source: `b7cb6cf5e7b5366a30bd12c350e2b394e8179b03`, version 2.4.1.

The owner requested About beside the account button with the version, developer and GitHub link.
The shared account controls now open the existing accessible dialog with the package version,
developer Adnan Rashid and https://github.com/clauding-lab/chess-prodigy. No network request is
needed to display the panel. Crowded controls wrap on small screens. The link opens separately;
the installed app's version remains accurate while an update is waiting for explicit acceptance.

## Verification

- TypeScript checks, lint, formatting and production build passed on Node 22.23.0.
- All 426 tests across 46 files passed.
- Hosted-feature browser checks: 9 passed, 3 existing intentional mobile skips. New desktop/mobile
  coverage opens About from Home, stacks it over setup, verifies keyboard focus and closing,
  opens it offline during a game and checks horizontal overflow.
- A separate disposable account on the local test server verified the signed-in mobile header,
  About content, closing and no horizontal overflow. No production account was used.
- Lighthouse: Wooden 100, Dark 100. About dialog axe checks: zero violations in both themes.
- Independent TypeScript/React code review approved with no findings.
- Linux candidate installed from the exact source archive on Node 22.22.2, typechecked and built
  with `VITE_PERSONALITY_BETA=true`. All 13 built file SHA-256 fingerprints match the local build.

The complete release check [GitHub run 34707565951](https://github.com/clauding-lab/chess-prodigy/actions/runs/34707565951)
passed: source checks, secret scan, 426 unit/integration tests and 116 browser tests, with eight
intentional skips in the default-off personality build.

## Live delivery

- Consistent private backup succeeded at 23:21:13 BDT.
- Activated `/opt/chess-prodigy/releases/2.4.1-20260912-b7cb6cf-about` at 23:22:24 BDT.
  Service active/running, exit status 0, restart count 0.
- At 23:23:02 BDT, HTTPS health returned 200 with no-store and all 13 public asset fingerprints
  matched the tested build. An isolated mobile-sized Chrome guest verified About's version,
  developer and GitHub link, dark default, a legal game, closing About and reload/Resume with
  the position intact. No page errors. No real player account or data was read.
- Source is pushed on main. This verification note is a later documentation-only commit;
  the deployed application source remains `b7cb6cf`. No new GitHub release tag was requested.

This update does not change engine behavior, Morphy versions or measured ratings, account policy,
saved-game formats or update consent. Dark remains the permanent default; saved Wooden preferences
remain supported. Browser emulation is not physical-device evidence.
