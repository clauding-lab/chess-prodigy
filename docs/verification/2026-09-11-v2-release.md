# Chess Prodigy 2.0.0 release record

Started 11 September 2026 BDT. The owner explicitly requested “push and cut a new
2.0 release. update the readme too” after reporting that the local app works.
This authorizes publishing the completed source and GitHub release. Deployment
and live player data are outside this release task.

## Starting state

- Repository/worktree: `/Users/adnanrashid/Projects/chess-prodigy` (persistent owner Mac).
- Branch: `codex/personality-pwa-run3`.
- Release preparation base: `f797a8c41095266ee8969c17169ac9d0ef36ff61`.
- Local and remote main: `f2b45b701795ea2aa03e89688ced04b8106b5928`.
- No pre-existing staged, modified or untracked files. No remote v2.0.0 tag.
- Existing published package/release version: 1.2.0. Main has no branch protection.
- Existing push workflow runs verification and secret scanning; it does not deploy.

## Checkpoints and required checks

1. Update README, changelog, version metadata and authorization; independently review
   this release diff. Preserve default-off, unrated Morphy and all existing data boundaries.
2. Run fresh typecheck, lint, formatting, full unit/integration suite, production build
   and full browser suite. Inspect and commit only task-owned release files.
3. Publish the verified source to main by fast-forward, without history rewriting.
   Wait for GitHub verification and committed-history secret scan to pass.
4. Create and push annotated v2.0.0 at the verified source, publish the stable GitHub
   release with reviewed notes, and verify the remote tag/release target.

## Migration and scope

Version 2 saves retain original v1 guest/account keys; legacy games become Classic.
Unreadable v2 saves never silently fall back. Once an account accepts v2, old v1-only
clients cannot overwrite it (HTTP 426). Preserve v2 saves and use compatible code for
rollback. Guest history is separate; account history remains private with pending
changes preserved. No SQL migration, automatic guest import or rating reset.

The completed Run 1–3 records contain the feature verification and independent data-path
reviews. Owner “all works” is local acceptance, not physical iPhone installation/audio
evidence. Physical-device checks, strength calibration and commercial naming clearance
remain outstanding. No additional personalities, Stockfish, paid services or analytics.

## Execution status

Documentation/version checkpoint prepared: README now leads with the 2.0 features and
guest beta instructions; changelog and GitHub release notes describe source-only scope.
Package and lockfile contain version 2.0.0, with no dependency changes. Independent
review corrected reproducibility wording to opening choices and limited new personality
seeds to Morphy rematches. Local documentation links and `git diff --check` pass.

Fresh checks on 11 September 2026 BDT, using Node 22.23.0:

- `npm run typecheck`, `npm run lint`, `npm run format:check`: pass.
- `npm test`: 321 passed across 38 files.
- `npm run build`: pass, default-off beta; offline bundle generated.
- Gitleaks 8.30.1, checksum verified: 33 committed-history commits scanned, no leaks.
- `npm run test:browser`: 88 passed, four existing intentional skips (three duplicate
  mobile account cases and the desktop touch-only sound case), 4.1 minutes.
- Staged-release Gitleaks scan: no leaks; package/lockfile version consistency passes.

All declared local release checks pass. The browser run regenerated four historical
theme screenshots; these are retained with the temporary logs and restored in Git,
so the release commit contains only the nine intended documentation/version files.
No test retries, hook bypasses or new skips. Full browser checks use disposable local
data; no live player data or deployment access. Previous beta-enabled, WebKit and
Lighthouse measurements remain explicitly dated in the Run 3 record, not claimed as
newly rerun here.

Logs: `/tmp/chess-prodigy-v2-release/`. No application-code changes in release preparation.
Next action: commit the verified release preparation, fast-forward/push main and wait
for GitHub checks before tagging and publishing. GitHub checks/publication pending.
Existing Run 3 recovery remains in the owner's Downloads folder; GitHub publication
will additionally preserve the committed source outside this Mac.
